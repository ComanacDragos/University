#pragma once
#include "Repository.h"
#include <fstream>
/*
class Service
{
private:
	Repository repo;

public:
	Service(const Repository& repo) :repo(repo){}

	void addHelicopter(int Id, const std::string& model, const std::string& activity, int maxAltitude, const std::string& isPrivate);
	void addPlane(int Id, const std::string& model, const std::string& activity, int maxAltitude, const std::string& isPrivate, const std::string& mainWings);
	void addBallon(int Id, const std::string& model, const std::string& activity, int maxAltitude, int weightLimit);
	std::vector<std::unique_ptr<Aircraft>>& getAircraftsActivity(const std::string activity);
	std::vector<std::unique_ptr<Aircraft>>& getAircraftsAltitude(int altitude);
};

*/